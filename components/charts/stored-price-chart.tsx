"use client"; import { useState } from "react"; import { CartesianGrid, Line, LineChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
export function StoredPriceChart({ data }: { data: Array<{ timestamp: string; raw: number; relative: number | null }> }) { const [mode,setMode]=useState<"relative"|"raw">("relative"); return <div><div className="mb-3 flex gap-2"><button className="rounded border border-[var(--border)] px-3 py-1" onClick={()=>setMode("relative")}>Normalized to 100</button><button className="rounded border border-[var(--border)] px-3 py-1" onClick={()=>setMode("raw")}>Raw price</button></div><div className="h-[360px]"><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><CartesianGrid stroke="var(--chart-grid)" strokeDasharray="3 3"/><XAxis dataKey="timestamp" minTickGap={40} tickFormatter={(value)=>String(value).slice(0,10)}/><YAxis domain={["auto","auto"]}/><Tooltip labelFormatter={(value)=>String(value)} formatter={(value)=>[Number(value).toFixed(4),mode==="raw"?"Adjusted price":"Performance"]}/><Line type="linear" dataKey={mode} dot={false} connectNulls={false} isAnimationActive={false} stroke="var(--accent)"/></LineChart></ResponsiveContainer></div></div>; }
